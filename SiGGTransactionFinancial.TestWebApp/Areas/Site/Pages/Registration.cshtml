@page
@using SiGGTransactionFinancial.Client.Models.Forms
@using SiGGTransactionFinancial.Common.HttpModels.Authentication
@using SiGGTransactionFinancial.Common.Models
@model Registration

@{
    ViewData["Title"] = "Registration";
}

<div class="text-center">
    <legend class="display-4">BodyGuard Registration Test</legend>
    <div class="site-form-container">
        @{
            await Html.RenderPartialAsync(
                                          "~/Views/Shared/_RegistrationForm.cshtml",
                                          new RegistrationForm
                                          {
                                              SuccessArea = "Site",
                                              SuccessPage = "/Success",
                                              FailureArea = "Site",
                                              FailurePage = "/Registration",
                                              SubmitLabel = "Registrati",
                                              CreateRequest = new UserCreateRequest {UserData = new User()}
                                          });
        }
    </div>
</div>